-- Tạo GUI
local player = game.Players.LocalPlayer
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "AutoMenu"
screenGui.Parent = player:WaitForChild("PlayerGui")

-- Khung chính
local frame = Instance.new("Frame")
frame.Size = UDim2.new(0, 220, 0, 340)
frame.Position = UDim2.new(0.5, -110, 0.5, -170)
frame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
frame.BackgroundTransparency = 0.2
frame.BorderSizePixel = 0
frame.Active = true
frame.Draggable = true
frame.Parent = screenGui

-- Tiêu đề
local title = Instance.new("TextLabel")
title.Size = UDim2.new(1, 0, 0, 25)
title.BackgroundTransparency = 1
title.Text = "AUTO MENU"
title.TextColor3 = Color3.new(1, 1, 1)
title.Font = Enum.Font.GothamBold
title.TextSize = 16
title.Parent = frame

-- Nút Auto Buy Pizza
local buyButton = Instance.new("TextButton")
buyButton.Size = UDim2.new(0.9, 0, 0, 30)
buyButton.Position = UDim2.new(0.05, 0, 0.08, 0)
buyButton.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
buyButton.Text = "Auto Buy Pizza: OFF"
buyButton.TextColor3 = Color3.new(1, 1, 1)
buyButton.Font = Enum.Font.Gotham
buyButton.TextSize = 14
buyButton.Parent = frame

-- Nút Auto Use Slot 1
local useButton1 = Instance.new("TextButton")
useButton1.Size = UDim2.new(0.9, 0, 0, 30)
useButton1.Position = UDim2.new(0.05, 0, 0.16, 0)
useButton1.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
useButton1.Text = "Auto Use Slot 1: OFF"
useButton1.TextColor3 = Color3.new(1, 1, 1)
useButton1.Font = Enum.Font.Gotham
useButton1.TextSize = 14
useButton1.Parent = frame

-- Nút Auto Use Slot 2
local useButton2 = Instance.new("TextButton")
useButton2.Size = UDim2.new(0.9, 0, 0, 30)
useButton2.Position = UDim2.new(0.05, 0, 0.24, 0)
useButton2.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
useButton2.Text = "Auto Use Slot 2: OFF"
useButton2.TextColor3 = Color3.new(1, 1, 1)
useButton2.Font = Enum.Font.Gotham
useButton2.TextSize = 14
useButton2.Parent = frame

-- Nút Auto Use Slot 3
local useButton3 = Instance.new("TextButton")
useButton3.Size = UDim2.new(0.9, 0, 0, 30)
useButton3.Position = UDim2.new(0.05, 0, 0.32, 0)
useButton3.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
useButton3.Text = "Auto Use Slot 3: OFF"
useButton3.TextColor3 = Color3.new(1, 1, 1)
useButton3.Font = Enum.Font.Gotham
useButton3.TextSize = 14
useButton3.Parent = frame

-- Nút Auto Teleport EternalOrb
local teleportButton = Instance.new("TextButton")
teleportButton.Size = UDim2.new(0.9, 0, 0, 30)
teleportButton.Position = UDim2.new(0.05, 0, 0.40, 0)
teleportButton.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
teleportButton.Text = "Auto Teleport EternalOrb: OFF"
teleportButton.TextColor3 = Color3.new(1, 1, 1)
teleportButton.Font = Enum.Font.Gotham
teleportButton.TextSize = 14
teleportButton.Parent = frame

-- Nút Auto Teleport Chaos (60s)
local chaosButton = Instance.new("TextButton")
chaosButton.Size = UDim2.new(0.9, 0, 0, 30)
chaosButton.Position = UDim2.new(0.05, 0, 0.48, 0)
chaosButton.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
chaosButton.Text = "Auto Teleport Chaos: OFF"
chaosButton.TextColor3 = Color3.new(1, 1, 1)
chaosButton.Font = Enum.Font.Gotham
chaosButton.TextSize = 14
chaosButton.Parent = frame

-- Nút Auto Teleport All Orbs
local orbButton = Instance.new("TextButton")
orbButton.Size = UDim2.new(0.9, 0, 0, 30)
orbButton.Position = UDim2.new(0.05, 0, 0.56, 0)
orbButton.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
orbButton.Text = "Auto Teleport All Orbs: OFF"
orbButton.TextColor3 = Color3.new(1, 1, 1)
orbButton.Font = Enum.Font.Gotham
orbButton.TextSize = 14
orbButton.Parent = frame

-- Biến trạng thái
local buyRunning = false
local useRunning1 = false
local useRunning2 = false
local useRunning3 = false
local teleportRunning = false
local chaosRunning = false
local orbRunning = false

-- Hàm dừng tất cả
local function stopAll()
    buyRunning = false
    useRunning1 = false
    useRunning2 = false
    useRunning3 = false
    teleportRunning = false
    chaosRunning = false
    orbRunning = false
    
    buyButton.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
    buyButton.Text = "Auto Buy Pizza: OFF"
    useButton1.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
    useButton1.Text = "Auto Use Slot 1: OFF"
    useButton2.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
    useButton2.Text = "Auto Use Slot 2: OFF"
    useButton3.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
    useButton3.Text = "Auto Use Slot 3: OFF"
    teleportButton.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
    teleportButton.Text = "Auto Teleport EternalOrb: OFF"
    chaosButton.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
    chaosButton.Text = "Auto Teleport Chaos: OFF"
    orbButton.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
    orbButton.Text = "Auto Teleport All Orbs: OFF"
end

-- Xử lý Auto Buy Pizza (giữ nguyên)
buyButton.MouseButton1Click:Connect(function()
    buyRunning = not buyRunning
    if buyRunning then
        buyButton.BackgroundColor3 = Color3.fromRGB(0, 255, 0)
        buyButton.Text = "Auto Buy Pizza: ON"
        task.spawn(function()
            while buyRunning do
                local args = {
                    "Buy5",
                    workspace:WaitForChild("Stalls"):WaitForChild("Punky Sky"):WaitForChild("Dragons' Faster Food"):WaitForChild("Shop"):WaitForChild("Pizza")
                }
                pcall(function()
                    game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("Effected"):FireServer(unpack(args))
                end)
                task.wait(0.0000001)
            end
        end)
    else
        buyButton.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
        buyButton.Text = "Auto Buy Pizza: OFF"
    end
end)

-- Các nút Auto Use Slot 1,2,3 (giữ nguyên như cũ)
useButton1.MouseButton1Click:Connect(function()
    useRunning1 = not useRunning1
    if useRunning1 then
        useButton1.BackgroundColor3 = Color3.fromRGB(0, 255, 0)
        useButton1.Text = "Auto Use Slot 1: ON"
        task.spawn(function()
            while useRunning1 do
                local args = { "UseItem", 1 }
                pcall(function()
                    game:GetService("ReplicatedStorage"):WaitForChild("Server"):FireServer(unpack(args))
                end)
                task.wait(0.01)
            end
        end)
    else
        useButton1.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
        useButton1.Text = "Auto Use Slot 1: OFF"
    end
end)

useButton2.MouseButton1Click:Connect(function()
    useRunning2 = not useRunning2
    if useRunning2 then
        useButton2.BackgroundColor3 = Color3.fromRGB(0, 255, 0)
        useButton2.Text = "Auto Use Slot 2: ON"
        task.spawn(function()
            while useRunning2 do
                local args = { "UseItem", 2 }
                pcall(function()
                    game:GetService("ReplicatedStorage"):WaitForChild("Server"):FireServer(unpack(args))
                end)
                task.wait(0.01)
            end
        end)
    else
        useButton2.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
        useButton2.Text = "Auto Use Slot 2: OFF"
    end
end)

useButton3.MouseButton1Click:Connect(function()
    useRunning3 = not useRunning3
    if useRunning3 then
        useButton3.BackgroundColor3 = Color3.fromRGB(0, 255, 0)
        useButton3.Text = "Auto Use Slot 3: ON"
        task.spawn(function()
            while useRunning3 do
                local args = { "UseItem", 3 }
                pcall(function()
                    game:GetService("ReplicatedStorage"):WaitForChild("Server"):FireServer(unpack(args))
                end)
                task.wait(0.0001)
            end
        end)
    else
        useButton3.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
        useButton3.Text = "Auto Use Slot 3: OFF"
    end
end)

-- Auto Teleport EternalOrb (giữ nguyên)
teleportButton.MouseButton1Click:Connect(function()
    teleportRunning = not teleportRunning
    if teleportRunning then
        teleportButton.BackgroundColor3 = Color3.fromRGB(0, 255, 0)
        teleportButton.Text = "Auto Teleport EternalOrb: ON"
        task.spawn(function()
            local orbIndex = 1
            while teleportRunning do
                local combatFolder = workspace:FindFirstChild("CombatFolder")
                if combatFolder then
                    local secretPlayer = combatFolder:FindFirstChild("secret_player1225")
                    if secretPlayer then
                        local eternalOrb = secretPlayer:FindFirstChild("EternalOrb")
                        if eternalOrb then
                            local parts = {}
                            for _, child in ipairs(eternalOrb:GetChildren()) do
                                if child:IsA("BasePart") then
                                    table.insert(parts, child)
                                end
                            end
                            if #parts == 0 and eternalOrb:IsA("BasePart") then
                                parts = { eternalOrb }
                            end

                            if #parts > 0 then
                                local orb = parts[orbIndex]
                                if orb then
                                    local character = player.Character
                                    if character and character:FindFirstChild("HumanoidRootPart") then
                                        character.HumanoidRootPart.CFrame = orb.CFrame
                                    end
                                end
                                orbIndex = orbIndex + 1
                                if orbIndex > #parts then
                                    orbIndex = 1
                                end
                            end
                        end
                    end
                end
                task.wait(0.0001)
            end
        end)
    else
        teleportButton.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
        teleportButton.Text = "Auto Teleport EternalOrb: OFF"
    end
end)

-- Auto Teleport Chaos (giữ nguyên)
chaosButton.MouseButton1Click:Connect(function()
    chaosRunning = not chaosRunning
    if chaosRunning then
        chaosButton.BackgroundColor3 = Color3.fromRGB(0, 255, 0)
        chaosButton.Text = "Auto Teleport Chaos: ON"
        task.spawn(function()
            local enemyIndex = 1
            while chaosRunning do
                local enemies = workspace:FindFirstChild("Enemies")
                if enemies then
                    local chaos = enemies:FindFirstChild("Chaos")
                    if chaos then
                        local enemyList = chaos:GetChildren()
                        local locations = {}
                        for _, enemy in ipairs(enemyList) do
                            local loc = enemy:FindFirstChild("EnemyLocation")
                            if loc and loc:IsA("BasePart") then
                                table.insert(locations, loc)
                            end
                        end
                        if #locations > 0 then
                            local target = locations[enemyIndex]
                            if target then
                                local character = player.Character
                                if character and character:FindFirstChild("HumanoidRootPart") then
                                    character.HumanoidRootPart.CFrame = target.CFrame
                                end
                            end
                            enemyIndex = enemyIndex + 1
                            if enemyIndex > #locations then
                                enemyIndex = 1
                            end
                        end
                    end
                end
                task.wait(60)
            end
        end)
    else
        chaosButton.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
        chaosButton.Text = "Auto Teleport Chaos: OFF"
    end
end)

-- ==================== PHẦN ĐÃ SỬA: AUTO TELEPORT ALL ORBS ====================
orbButton.MouseButton1Click:Connect(function()
    orbRunning = not orbRunning
    if orbRunning then
        orbButton.BackgroundColor3 = Color3.fromRGB(0, 255, 0)
        orbButton.Text = "Auto Teleport All Orbs: ON"
        task.spawn(function()
            local orbIndex = 1
            while orbRunning do
                local combatFolder = workspace:FindFirstChild("CombatFolder")
                if combatFolder then
                    local secretPlayer = combatFolder:FindFirstChild("secret_player1225")
                    if secretPlayer then
                        -- Thu thập TẤT CẢ các Orb (giống cấu trúc EternalOrb)
                        local orbs = {}
                        for _, model in ipairs(secretPlayer:GetChildren()) do
                            if model.Name:find("Orb") then
                                -- Lấy tất cả BasePart bên trong mỗi Orb
                                for _, child in ipairs(model:GetChildren()) do
                                    if child:IsA("BasePart") then
                                        table.insert(orbs, child)
                                    end
                                end
                                -- Nếu chính model là BasePart thì thêm luôn
                                if model:IsA("BasePart") then
                                    table.insert(orbs, model)
                                end
                            end
                        end

                        if #orbs > 0 then
                            local target = orbs[orbIndex]
                            if target then
                                local character = player.Character
                                if character and character:FindFirstChild("HumanoidRootPart") then
                                    character.HumanoidRootPart.CFrame = target.CFrame
                                end
                            end
                            orbIndex = orbIndex + 1
                            if orbIndex > #orbs then
                                orbIndex = 1
                            end
                        end
                    end
                end
                task.wait(0.00000000000000000000000000000001)  -- spam teleport
            end
        end)
    else
        orbButton.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
        orbButton.Text = "Auto Teleport All Orbs: OFF"
    end
end)

-- Nút đóng
local closeButton = Instance.new("TextButton")
closeButton.Size = UDim2.new(0, 20, 0, 20)
closeButton.Position = UDim2.new(1, -20, 0, 0)
closeButton.BackgroundColor3 = Color3.fromRGB(255, 50, 50)
closeButton.Text = "X"
closeButton.TextColor3 = Color3.new(1, 1, 1)
closeButton.Font = Enum.Font.GothamBold
closeButton.TextSize = 14
closeButton.Parent = frame
closeButton.MouseButton1Click:Connect(function()
    stopAll()
    screenGui:Destroy()
end)
